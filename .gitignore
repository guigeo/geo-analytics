.DS_Store
__pycache__/
*.pyc
.env
.env.local
node_modules/

# Python / uv
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Frontend build
web/dist/

# Dados (crus + processados): grandes/reproduziveis — nao versionar.
# Mantem apenas o README explicando onde obter as fontes.
data/*
!data/README.md

# Saidas do ETL (geradas; nao versionar)
web/src/search/municipios.json

# Tiles nao moram mais neste repositorio: vivem no host compartilhado
# (TILES_DIR no .env; ver ../webgis/docs/LOCAL.md). Um .pmtiles aparecendo aqui
# significa que alguem recriou a copia por aplicacao.
*.pmtiles
